# リリース署名のテンプレート。
# 実値はこのファイルをコピーした keystore.properties(gitignore 対象)に記述する。
# storeFile はキーストアの絶対パス(リポジトリ外を推奨)。
storeFile=/path/to/your/release.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=release
keyPassword=YOUR_KEY_PASSWORD
